import { RouteErrorCode, SystemErrorCode } from './types';

export default {
  // Middleware
  [SystemErrorCode.UNHANDLED_SYSTEM_ERROR]: 'Unhandled system error.',
  // Routes
  [RouteErrorCode.INVALID_PATH]: 'Invalid path.',
  // Models
};
